# Archive — 过时与历史文档

本目录存放已不再反映当前实现的文档。分为三类：

## 过时文档（OUTDATED）

内容已与代码脱节，保留供参考但**不可作为实现依据**。

| 文件 | 原因 |
|------|------|
| `alpha-status-v0.1.md` | v0.1.0-alpha 状态快照（566 tests），当前已达 1059 tests，能力清单严重过时 |
| `contracts-v1-era.md` | 仅列出 13 个 v1 合约，后续新增 40+ 合约未收录 |
| `delegation-policy-unimplemented.md` | 描述的委托角色（rp-critic 等）和模式从未实现；实际使用 D1-D6 + Wave A/B 调度 |

## 交付报告（delivery-reports）

里程碑交付记录，测试基线已大幅超越。

## Handoffs（handoffs）

各阶段交接文档，均已完成。测试数为当时快照。

## Decisions（decisions）

已执行完毕的 ADR。

---

> 如需查看当前架构，请回上级目录的 `architecture/` 和 `superpowers/`。
